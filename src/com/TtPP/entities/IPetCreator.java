package com.TtPP.entities;

public interface IPetCreator {
    /**
     * In real world here we should have something like IPet interface and return an instance that implements IPet,
     * this test app does not have such need.
     */
    Pet create ();
}
